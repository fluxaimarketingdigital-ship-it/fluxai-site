# STG-05R: BLOCO A — GATE 9 — SEEDS APLICADOS

A migration auxiliar `20260618000003_seeds.sql` introduziu os inserts básicos.
* Tabela `profiles`: 1 registro (`STG_USER_ADMIN` no perfil ADMIN).
* Tabela `crm_leads`: 1 registro (`STG_LEAD_FICTICIO`).

As linhas carregadas representam exclusivamente metadados estáticos para sustentar testes visuais e SQL da base. Nenhuma vinculação externa (URL real ou WhatsApp) injetada. Os IDs mantiveram os UUIDs Mock do Gate 8.
